# Datasets

Place local raster, vector, weather, solar irradiance, wind atlas, road, substation, and transmission datasets here.

The application runs without external datasets because the backend includes deterministic coordinate-based environmental and prediction logic.

version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - renewable_data:/app/instance
    environment:
      - SECRET_KEY=prod-secret-key-change-me
    restart: always

volumes:
  renewable_data:
